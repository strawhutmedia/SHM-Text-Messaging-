{
  "name": "shm-imessage-bridge",
  "version": "0.1.0",
  "description": "Bridges GoHighLevel conversations to iMessage via a BlueBubbles server running on a Mac",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/store.js && node --check src/ghl.js && node --check src/bluebubbles.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "dotenv": "^16.4.0",
    "express": "^4.19.0"
  }
}
